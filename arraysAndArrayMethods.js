function doubleAll(numArr){return numArr.map(num => num*4)};
function sumArray(numArr){return numArr.reduce((acc, curr) => acc + curr, 0)};
function filterEven(numArr){return  numArr.filter(num => num % 2 == 1)};
